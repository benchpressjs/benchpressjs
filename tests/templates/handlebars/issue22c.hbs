{{#each animals as |animals__item animals__key|}}
{{esc this.name}} {{#if this.hobbies.length}}has{{else}}has no{{/if}} hobbies
{{/each}}

{{#each animals as |animals__item animals__key|}}
{{esc this.name}} {{#if this.hobbies.length}}has{{else}}has no{{/if}} hobbies
{{/each}}