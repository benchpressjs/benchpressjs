{{#each animals as |animals__item animals__key|}}
{{this.name}} {{#if this.hobbies.length}}has{{else}}has no{{/if}} hobbies
{{/each}}

{{#each programmers as |programmers__item programmers__key|}}
{{this.name}} {{#if this.hobbies.length}}has{{else}}has no{{/if}} hobbies
{{/each}}

{{#each animals as |animals__item animals__key|}}
{{this.name}} {{#if this.hobbies.length}}has{{else}}has no{{/if}} hobbies
{{/each}}